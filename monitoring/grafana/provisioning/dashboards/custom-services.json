{
  "dashboard": {
    "title": "Cloud 3-Tier Services",
    "panels": [
      {
        "title": "API Request Rate",
        "targets": [
          {
            "expr": "sum(rate(nginx_http_requests_total[1m])) by (exported_namespace)",
            "legendFormat": "{{ exported_namespace }}"
          }
        ]
      }
    ]
  }
}
